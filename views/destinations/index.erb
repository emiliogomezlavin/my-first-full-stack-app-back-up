<a class="btn-floating btn-large waves-effect waves-light red darken-4" href="/destinations/new"><i class="material-icons">add</i></a>
<div class="row">
  <div class="col s12 center-align">
    <h1>Destinations</h1>
  </div>
</div>
<div class="divider" style="margin-bottom: 65px"></div>

<div class="row">
  <% @destinations.each do |destination| %>
  <div class="col s6 destImg">
    <div class="card">
      <div class="card-image waves-effect waves-block waves-light">
        <a href="/destinations/<%= destination.id %>">
          <img class="activator" src="<%= destination.photo_url %>" alt="<%= destination.name %> photo">
        </a>
      </div>
      <div class="card-content">
        <span class="card-title activator grey-text text-darken-4">
          <a href="/destinations/<%= destination.id %>"><%= destination.name %></a><i class="material-icons right">more_vert</i>
        </span>
      </div>
      <div class="card-reveal">
        <span class="card-title grey-text text-darken-4 center-align">
          <a href="/destinations/<%= destination.id %>"><%= destination.name %></a><i class="material-icons right">close</i>
        </span>
        <p>Here is some more information about this destination that is only revealed once clicked on.</p>
        <div class="row">        
          <div class="col s6">
            <a class="waves-effect waves-light btn" href="/destinations/<%= destination.id %>/edit"><i class="material-icons right">mode_edit</i>Edit</a>
          </div>
          <div class="col s6">
            <form method="post" action="/destinations/<%= destination.id %>/delete">
              <button class="btn waves-effect waves-light light-blue darken-1" type="submit" name="action"><i class="material-icons right">delete</i>Delete</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>  
  <% end %>
</div>

<!-- 
<div class="destinations">
  <% @destinations.each do |destination| %>
  <div class="row">
    <div class="col-xs-2 col-xs-offset-3"> 
      <a href="/destinations/<%= destination.id %>">
        <img id="destImg" src="<%= destination.photo_url %>" alt="<%= destination.name %> photo" height="200" width="300">
      </a>
    </div>
    <div class="col-xs-4 col-xs-offset-2">
      <ul>
        <li>
          <h2>
            <a href="/destinations/<%= destination.id %>">
              <%= destination.name %>
            </a>
          </h2>
        </li>
        <div class="row">
          <div class="col-xs-2">  
            <li>
              <a class="btn btn-primary" href="/destinations/<%= destination.id %>/edit">
                Edit <span class="glyphicon glyphicon-pencil" aria-hidden="true" style="padding-left: 6px"></span> 
              </a>
            </li>
          </div>
          <div class="col-xs-2 col-xs-offset-1">    
            <li>
              <li>
                <form method="post" action="/destinations/<%= destination.id %>/delete">
                  <button class="btn btn-danger">Delete  <span class="glyphicon glyphicon-trash" aria-hidden="true" style="padding-left: 6px"></span> </button>
                </form>
              </li>
            </li>
          </div>
        </div>  
      </ul>
    </div>  
  </div>
  <% end %>
</div>  -->